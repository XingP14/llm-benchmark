name: CI

# 避免同 branch 上多次 push 并发跑导致 npm ci cache 冲突
# (与 6741027 docker.yml 修法同模式, 后 push 取消前 push, 同一 ref 串行)
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

on:
  push:
    branches: [master, main]
  pull_request:
    branches: [master, main]

jobs:
  test:
    name: Lint + Test (Node ${{ matrix.node-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node-version: [20.x]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint --if-present

      - name: Build
        run: npm run build --if-present

      - name: Test
        # 不用 npm test 因为 package.json scripts.test 启用了 --coverage，
        # 干干 90% 达不到 95% 阈值会 block CI。CI 验证功能，coverage 在本地
        # 手动跑 npm test 检查。
        run: npx jest --runInBand --bail --forceExit
